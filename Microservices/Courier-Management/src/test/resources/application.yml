spring:
  application:
    name: courier-management
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:5432/deliverydb_test
    username: postgres
    password: postgres